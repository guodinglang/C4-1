# 分段电压校准算法简介

## 1. 系统概述

本系统采用**60段分段校准**方案，将0-60V电压范围划分为60个区间（每段1V），对每个区间独立校准，提高全量程输出精度。

## 2. 核心数据结构

```c
// 校准参数结构
typedef struct{
    int16_t  k[4][60];       // 校准系数 [通道][分段], 保留3位小数
    int16_t  ol[4][60];      // 偏移量 [通道][分段], 保留3位小数
    uint8_t  seg_valid[4][8];// 有效标记位图 [通道][8字节=64位]
    uint8_t  seg_enable;     // 分段使能标志
} ParameterSturct;
```

## 3. 校准原理

### 3.1 误差模型
输出电压误差主要由两部分组成：
- **增益误差**：比例系数偏差
- **偏移误差**：零点漂移

### 3.2 校准公式
```
Vout = Vin - offset - (Vin × k)

其中:
- Vin: 目标输出电压
- k: 校准系数 (存储值/1000)
- offset: 偏移量 (存储值/1000)
- Vout: DAC实际输出电压
```

### 3.3 系数计算
通过测量实际输出与理论值的偏差计算k系数：
```
k = (V实际 - V理论 - offset) / V理论
```

## 4. 分段插值算法

### 4.1 问题
如果某段未校准（seg_valid=0），直接使用会导致精度下降。

### 4.2 解决方案：线性插值
取左右两侧最近的有效段进行线性插值：

```
k插值 = k左 + (k右 - k左) × (V - V左) / (V右 - V左)
```

### 4.3 算法流程
1. 根据电压值确定所在分段 `seg = (uint8_t)(voltage)`
2. 检查当前段是否有效（`IsSegmentValid(ch, seg)`）
3. 若有效，直接使用该段k和ol
4. 若无效，查找左右最近有效段进行线性插值

## 5. 算法实现

### 5.1 核心函数

| 函数 | 功能 |
|------|------|
| `GetSegmentIndex(voltage)` | 根据电压获取分段索引 |
| `IsSegmentValid(ch, seg)` | 检查段是否已校准 |
| `GetInterpolatedK(ch, seg, voltage)` | 获取插值后的k系数 |
| `GetInterpolatedOffset(ch, seg, voltage)` | 获取插值后的偏移量 |

### 5.2 插值算法代码
```c
int16_t GetInterpolatedK(uint8_t ch, uint8_t seg, double voltage)
{
    // 当前段有效，直接返回
    if(IsSegmentValid(ch, seg)) 
        return params.k[ch][seg];
    
    // 查找左右最近的有效段
    left = 查找左侧最近有效段(seg);
    right = 查找右侧最近有效段(seg);
    
    // 线性插值
    ratio = (voltage - left) / (right - left);
    return k左 + (k右 - k左) × ratio;
}
```

## 6. 存储结构

```
Flash Sector 122 (INTERIM):
+--------+--------+--------------------------------+
|  地址  |  大小  |              内容               |
+--------+--------+--------------------------------+
| 0      | 480B   | k[4][60] 校准系数              |
| 480    | 480B   | ol[4][60] 偏移量               |
| 960    | 25B    | ver[25] 版本信息               |
| 985    | 32B    | seg_valid[4][8] 有效标记位图   |
+--------+--------+--------------------------------+
```

## 7. 使用方法

### 7.1 启用分段校准
```
ofs en 1
```

### 7.2 单点校准（cbs命令）
```
cbs <通道> <分段> <理论值> <实测值>

示例：校准通道1的第10分段（10V附近）
cbs 1 10 10.000 9.985
```

### 7.3 查看校准系数
```
ofs rks <通道> <分段>

示例：查看通道1第10段的k系数
ofs rks 1 10
```

### 7.4 直接写入系数（高级）
```
ofs wks <通道> <分段> <k系数>
ofs wls <通道> <分段> <偏移量>

示例：
ofs wks 1 10 15.0    // k=0.015
ofs wls 1 10 5.0     // offset=0.005
```

## 8. 校准流程建议

### 8.1 首次校准
1. 启用分段校准：`ofs en 1`
2. 选择关键电压点（建议每5V或10V校准一个点）
3. 使用万用表测量实际输出
4. 执行cbs命令记录偏差
5. 系统自动插值中间点

### 8.2 校准验证
设置不同电压值，用万用表验证实际输出精度。

## 9. 精度分析

- **直接校准段**：精度取决于万用表精度（通常±0.1%）
- **插值段**：精度取决于相邻段线性度（通常±0.5%）
- **全量程精度**：优于±1%（0-60V范围）

## 10. 注意事项

1. k系数可为负数，表示实际输出偏高
2. 偏移量ol独立存储，可与k系数配合使用
3. 插值算法在边界处（0V和60V）仅使用单侧数据
4. 首次烧录需修改初始化标记（0x31→0x32）触发参数重置
